# Sitemap Generator || NextJS & Firebase

## Installation

```bash
npm install
```

- Update `config/firebase.json`. Fill all keys with your firebase information.
- Change `.env.example` with `.env` and update `*FIREBASE_DB_URL*` with your information

## Usage

```bash
node app.js
```